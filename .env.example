# Export these in your shell; the CLI does not automatically load .env files.
PAGEVOICE_DATA=.
# XTTS model terms must be reviewed and accepted by the operator at first use.
# PageVoice never sets COQUI_TOS_AGREED for you.

# Local server port; use another port if this one is occupied.
PAGEVOICE_PORT=8765
